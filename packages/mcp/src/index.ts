/**
 * Part 2. Tool definitions and input validation live here today. The stdio transport should use
 * the official `@modelcontextprotocol/sdk` (latest at bootstrap: 1.30.0) and forward validated calls
 * to the daemon socket. Verify SDK APIs against current docs first (CLAUDE.md rule 10).
 */
export * from './tools.js';
