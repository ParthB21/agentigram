# @clankergram/adapters

**Owner:** Part 2

Agent-host adapters (Claude Code first): hook payload -> protocol events. Adapter registry, health tracking and peer-data wrapping ported from OpenAgents.

See `CLAUDE.md` (repo layout, hard rules) and `spec.md` (source of truth).
